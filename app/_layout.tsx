/*
import { Stack } from 'expo-router';

export default function RootLayout() {
  return (
    <Stack>
      <Stack.Screen name="(tabs)/index" />
      <Stack.Screen name="(tabs)/Settings" options={{ headerShown: false, statusBarColor: 'black' }} />
    </Stack>
  );
*/